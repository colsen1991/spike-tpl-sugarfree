{
  "name": "<%= S(name).slugify().value() %>",
  "description": "<%= description %>",
  "version": "0.0.1",
  "author": "Christer Olsen <name@example.com> (https://github.com/<%= github_username %>)",
  "bugs": "https://github.com/<%= github_username %>/<%= S(name).slugify().value() %>/issues",
  "dependencies": { },
  "devDependencies": {
    "ava": "^0.19.0",
    "babel-eslint": "^7.2.3",
    "babel-preset-env": "^1.3.2",
    "eslint": "^4.0.0",
    "marked": "^0.3.6",
    "noop-webpack-plugin": "^1.0.1",
    "reshape-standard": "^1.1.0",
    "spike": "^1.3.1",
    "spike-css-standards": "^1.1.0",
    "webpack": "^3.0.0"
  },
  "homepage": "https://github.com/<%= github_username %>/<%= S(name).slugify().value() %>",
  "main": "app.js",
  "private": true,
  "license": "MIT",
  "repository": "https://github.com/<%= github_username %>/<%= S(name).slugify().value() %>",
  "scripts": {
    "lint": "eslint assets/js",
    "test": "export NODE_ENV=test && ava -v",
    "build": "export NODE_ENV=production && spike clean && spike compile -e production",
    "netlify": "npm i && npm run build",
    "dev": "export NODE_ENV=development && spike clean && spike watch -e development"
  }
}
